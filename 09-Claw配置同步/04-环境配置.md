# 04 - 环境配置

## 📋 环境概述

**运行环境**：AnyDev 开发机（Linux容器）  
**操作系统**：TencentOS Server 4.0（基于CentOS/RHEL）  
**架构**：x86_64  
**工作目录**：`/data/workspace`

---

## 🐍 Python 环境

### Python 版本

```bash
python3 --version
# Python 3.x.x
```

### 核心依赖库

| 库名称 | 版本建议 | 用途 | 安装命令 |
|--------|---------|------|---------|
| **pandas** | >=1.3.0 | 数据处理 | `pip3 install pandas` |
| **openpyxl** | >=3.0.0 | Excel读写 | `pip3 install openpyxl` |
| **yfinance** | >=0.2.0 | 美股数据 | `pip3 install yfinance` |
| **requests** | >=2.28.0 | HTTP请求 | `pip3 install requests` |
| **futu-api** | >=6.3.5308 | 富途OpenAPI | `pip3 install futu-api` |
| **python-docx** | >=0.8.11 | Word文档 | `pip3 install python-docx` |
| **python-pptx** | >=0.6.21 | PPT创建 | `pip3 install python-pptx` |
| **beautifulsoup4** | >=4.11.0 | HTML解析 | `pip3 install beautifulsoup4` |
| **lxml** | >=4.9.0 | XML解析 | `pip3 install lxml` |
| **matplotlib** | >=3.5.0 | 数据可视化 | `pip3 install matplotlib` |
| **seaborn** | >=0.12.0 | 统计可视化 | `pip3 install seaborn` |
| **plotly** | >=5.10.0 | 交互式图表 | `pip3 install plotly` |

### 完整安装命令

```bash
pip3 install pandas openpyxl yfinance requests futu-api python-docx python-pptx beautifulsoup4 lxml matplotlib seaborn plotly
```

### 验证安装

```python
# 验证核心库
python3 << 'EOF'
import pandas as pd
import yfinance as yf
import requests
import openpyxl
from docx import Document
from pptx import Presentation

print("✅ 所有核心库已安装")
print(f"pandas: {pd.__version__}")
print(f"yfinance: {yf.__version__}")
EOF
```

---

## 📂 目录结构

### 工作目录树

```
/data/workspace/
├── scripts/                          # 脚本目录
│   ├── update_stock_2.0_v4.py       # 美股2.0更新脚本（最新）
│   ├── update_stock_2.0.py          # 美股2.0更新脚本（V3）
│   ├── update_us_stocks.py          # 美股更新脚本（遗留）
│   ├── sync_ai_report_to_git.py     # AI报告GitHub同步
│   ├── generate_poster.py           # PPTX海报生成
│   ├── convert_docx_to_md.py        # DOCX转Markdown
│   ├── upload_to_lexiang.py         # 乐享上传工具
│   ├── upload_to_lexiang_batch.py   # 乐享批量上传
│   ├── mac_sync_to_lexiang.py       # Mac端同步脚本
│   ├── sync_investment_to_git.py    # 投资数据GitHub同步
│   ├── test_stock_v4.py             # V4系统测试
│   └── test_yfinance_only.py        # yfinance测试
│
├── stock_pool/                       # 股票池目录
│   ├── 美股投资标的_跟踪2.0.xlsx      # 美股2.0数据（32只）
│   ├── AH股投资标的_跟踪2.0.xlsx      # AH股数据
│   ├── 美股投资标的2.0-配置说明.md    # 配置文档
│   └── 美股投资标的-使用说明.md       # 使用文档（遗留）
│
├── ai_innovation_reports/            # AI创新报告目录
│   ├── AI创新报告_2026-02-25.md
│   ├── AI创新报告_2026-02-25_海报.pptx
│   └── ...
│
├── weekly_market_insights/           # 市场洞察报告目录
│   ├── 市场投资洞察报告_2026-02-21.docx
│   └── ...
│
├── ai-innovation-resources/          # GitHub仓库（本地克隆）
│   ├── 07-团队分享/
│   │   └── AI创新报告/
│   ├── 08-投资跟踪/
│   │   ├── AH股投资标的/
│   │   └── 美股投资标的/
│   └── 09-Claw配置同步/             # 本配置文档
│       ├── README.md
│       ├── 01-定时任务配置.md
│       ├── 02-Skill使用指南.md
│       ├── 03-记忆体信息.md
│       ├── 04-环境配置.md
│       └── 05-快速迁移指南.md
│
├── claw-config/                      # 配置文档工作目录
│   └── [同上]
│
├── 美股2.0更新报告_*.md               # 更新报告（带时间戳）
├── 美股2.0数据源切换说明_V3.md        # 技术文档
├── 富途OpenD配置指南.md               # 富途配置
├── 投资跟踪文件同步到乐享操作指南.md    # 乐享同步指南
└── ...
```

---

## 🔧 关键脚本清单

### 投资跟踪脚本

| 脚本名称 | 路径 | 用途 | 调用频率 |
|---------|------|------|---------|
| update_stock_2.0_v4.py | /data/workspace/scripts/ | 美股2.0更新（V4最新） | 每天6:00 |
| update_stock_2.0.py | /data/workspace/scripts/ | 美股2.0更新（V3） | 已弃用 |
| update_us_stocks.py | /data/workspace/scripts/ | 美股更新（遗留） | 每天5:00 |
| test_stock_v4.py | /data/workspace/scripts/ | V4系统测试 | 手动 |
| test_yfinance_only.py | /data/workspace/scripts/ | yfinance测试 | 手动 |

### 报告生成脚本

| 脚本名称 | 路径 | 用途 | 调用频率 |
|---------|------|------|---------|
| generate_poster.py | /data/workspace/scripts/ | PPTX海报生成 | 每天8:00 |
| convert_docx_to_md.py | /data/workspace/scripts/ | DOCX转Markdown | 手动 |

### 同步工具脚本

| 脚本名称 | 路径 | 用途 | 调用频率 |
|---------|------|------|---------|
| sync_ai_report_to_git.py | /data/workspace/scripts/ | AI报告同步到GitHub | 每天8:00 |
| sync_investment_to_git.py | /data/workspace/scripts/ | 投资数据同步到GitHub | 手动 |
| upload_to_lexiang.py | /data/workspace/scripts/ | 单文件上传到乐享 | 手动 |
| upload_to_lexiang_batch.py | /data/workspace/scripts/ | 批量上传到乐享 | 手动 |
| mac_sync_to_lexiang.py | /data/workspace/scripts/ | Mac端同步工具 | Mac本地 |

---

## 🔑 环境变量与配置

### GitHub 配置

**用于自动推送到 GitHub**：

```bash
# Git 用户配置
git config --global user.name "dyc712"
git config --global user.email "dongyunchuan712@gmail.com"

# 存储凭证（已配置Personal Access Token）
git config --global credential.helper store
```

### 乐享 API 配置

**注意**：由于 AnyDev 网络限制，乐享 API 无法直接访问

**凭证信息**（用于Mac本地）：
```python
LEXIANG_APP_KEY = "270a7c98f13511f0aa25a64b7d33bf59"
LEXIANG_APP_SECRET = "c7cIlryYVq1p9lDq3vav7X1NXIAPM10OFbDghRbV"
LEXIANG_STAFF_ID = "joeydong"
```

### 富途 OpenAPI 配置

**连接配置**（需Mac本地运行FutuOpenD）：

```python
FUTU_HOST = "127.0.0.1"
FUTU_PORT = 11111
```

**通过SSH隧道连接**：
```bash
ssh -L 11111:127.0.0.1:11111 -N -f user@mac_ip
```

---

## 🌐 网络环境说明

### AnyDev 开发机网络限制

| 服务/网站 | 可访问性 | 说明 |
|----------|---------|------|
| **GitHub** | ✅ 可用 | 正常推送和拉取 |
| **yfinance** | ✅ 可用 | 美股数据主要来源 |
| **Finnhub API** | ❌ 不可用 | SSL连接错误 |
| **富途 OpenD** | ❌ 不可用 | SSL连接错误，需Mac本地 |
| **乐享 API** | ❌ 不可用 | DNS解析失败 |
| **Yahoo Finance** | ✅ 可用 | 通过yfinance访问 |
| **一般HTTP网站** | ✅ 可用 | 可以访问 |

### 数据源选择建议

**基于网络限制的推荐方案**：

```
主数据源: yfinance（95%+成功率）
实时数据: 富途 OpenAPI（需Mac本地 + SSH隧道）
历史回测: Polygon.io（如可访问）
```

---

## 📦 依赖管理

### requirements.txt

创建依赖文件：

```bash
cat > /data/workspace/requirements.txt << 'EOF'
pandas>=1.3.0
openpyxl>=3.0.0
yfinance>=0.2.0
requests>=2.28.0
futu-api>=6.3.5308
python-docx>=0.8.11
python-pptx>=0.6.21
beautifulsoup4>=4.11.0
lxml>=4.9.0
matplotlib>=3.5.0
seaborn>=0.12.0
plotly>=5.10.0
EOF
```

### 安装所有依赖

```bash
pip3 install -r /data/workspace/requirements.txt
```

### 导出当前环境

```bash
pip3 freeze > /data/workspace/requirements_frozen.txt
```

---

## 🔄 环境迁移

### 在新 Claw 实例中重建环境

**步骤1：克隆GitHub仓库**
```bash
cd /data/workspace
git clone https://github.com/dyc712/ai-innovation-resources.git
```

**步骤2：安装Python依赖**
```bash
pip3 install -r /data/workspace/requirements.txt
```

**步骤3：创建目录结构**
```bash
mkdir -p /data/workspace/{scripts,stock_pool,ai_innovation_reports,weekly_market_insights,claw-config}
```

**步骤4：复制脚本文件**
```bash
# 从GitHub仓库复制（如果有）
# 或手动创建脚本文件
```

**步骤5：配置Git**
```bash
git config --global user.name "dyc712"
git config --global user.email "dongyunchuan712@gmail.com"
```

**步骤6：验证环境**
```bash
# 测试Python依赖
python3 -c "import pandas, yfinance, requests; print('✅ 环境就绪')"

# 测试脚本执行
python3 /data/workspace/scripts/test_yfinance_only.py
```

---

## 🧪 环境测试

### 快速测试脚本

创建测试脚本：

```python
#!/usr/bin/env python3
"""环境验证脚本"""
import sys

def test_imports():
    """测试核心库导入"""
    print("🧪 测试核心库导入...")
    
    try:
        import pandas as pd
        print(f"  ✅ pandas {pd.__version__}")
    except ImportError as e:
        print(f"  ❌ pandas: {e}")
        return False
    
    try:
        import yfinance as yf
        print(f"  ✅ yfinance {yf.__version__}")
    except ImportError as e:
        print(f"  ❌ yfinance: {e}")
        return False
    
    try:
        import requests
        print(f"  ✅ requests {requests.__version__}")
    except ImportError as e:
        print(f"  ❌ requests: {e}")
        return False
    
    return True

def test_directories():
    """测试目录结构"""
    import os
    print("\n🧪 测试目录结构...")
    
    required_dirs = [
        '/data/workspace/scripts',
        '/data/workspace/stock_pool',
        '/data/workspace/ai_innovation_reports'
    ]
    
    all_exist = True
    for dir_path in required_dirs:
        if os.path.exists(dir_path):
            print(f"  ✅ {dir_path}")
        else:
            print(f"  ❌ {dir_path} (不存在)")
            all_exist = False
    
    return all_exist

def test_data_sources():
    """测试数据源连接"""
    print("\n🧪 测试数据源连接...")
    
    # 测试 yfinance
    try:
        import yfinance as yf
        ticker = yf.Ticker("AAPL")
        info = ticker.info
        if info:
            print(f"  ✅ yfinance 可用")
        else:
            print(f"  ⚠️  yfinance 可用但数据为空")
    except Exception as e:
        print(f"  ❌ yfinance: {e}")
        return False
    
    return True

def main():
    print("=" * 60)
    print("Claw 环境验证")
    print("=" * 60)
    
    results = [
        test_imports(),
        test_directories(),
        test_data_sources()
    ]
    
    print("\n" + "=" * 60)
    if all(results):
        print("✅ 环境验证通过")
        return 0
    else:
        print("❌ 环境验证失败，请检查上述错误")
        return 1

if __name__ == "__main__":
    sys.exit(main())
```

**保存并运行**：
```bash
python3 /data/workspace/scripts/test_environment.py
```

---

## 📊 性能优化

### Python 优化

```python
# 使用 pandas 的优化选项
import pandas as pd
pd.options.mode.chained_assignment = None  # 关闭警告
pd.set_option('display.max_columns', None)  # 显示所有列
```

### 内存优化

```python
# 处理大型 Excel 文件时
import pandas as pd

# 分块读取
chunks = pd.read_excel('large_file.xlsx', chunksize=1000)
for chunk in chunks:
    process(chunk)
```

### 网络优化

```python
# yfinance 超时设置
import yfinance as yf

ticker = yf.Ticker("AAPL")
ticker.history(period="1d", timeout=60)  # 60秒超时
```

---

## 🔍 故障排查

### 常见问题

**问题1：pandas 读取 Excel 失败**
```bash
# 安装 openpyxl 引擎
pip3 install openpyxl
```

**问题2：yfinance 连接超时**
```python
# 增加超时时间
ticker.history(period="1d", timeout=120)
```

**问题3：Git 推送失败**
```bash
# 检查凭证配置
git config --global credential.helper store

# 重新输入 Personal Access Token
git push origin main
```

**问题4：脚本权限不足**
```bash
# 添加执行权限
chmod +x /data/workspace/scripts/*.py
```

---

## 🔗 相关资源

### 文档链接
- Python官方文档：https://docs.python.org/3/
- pandas文档：https://pandas.pydata.org/docs/
- yfinance文档：https://github.com/ranaroussi/yfinance

### GitHub仓库
- 配置仓库：https://github.com/dyc712/ai-innovation-resources
- 配置路径：`09-Claw配置同步/`

### 其他配置文档
- `01-定时任务配置.md` - 定时任务详情
- `02-Skill使用指南.md` - Skill配置
- `03-记忆体信息.md` - 记忆体管理
- `05-快速迁移指南.md` - 迁移步骤

---

**文档维护者**：AI Assistant  
**最后更新**：2026-02-25
